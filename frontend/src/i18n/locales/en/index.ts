import { common } from './common';
import { navigation } from './navigation';
import { settings } from './settings';
import { dashboard } from './dashboard';
import { en as finance } from '../../../modules/finance/locales/en';

export const en = {
  common,
  navigation,
  settings,
  dashboard,
  finance,
};

